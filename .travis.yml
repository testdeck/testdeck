language: node_js
node_js:
  - "node"
  - "10"
  - "9"
  - "8"
sudo: false
cache:
  directories:
    - node_modules
install:
  - test -d node_modules && npm prune || true
  - npm run setup
script:
  - npm run test
#branches:
#  except:
#    - /^gh-.*$/
