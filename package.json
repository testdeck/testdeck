{
  "name": "mocha-typescript-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "TypeScript decorators based wrapper over mocha's interface",
  "author": "Panayot Cankov",
  "contributors": [
    {
      "name": "silkentrance",
      "url": "https://github.com/silkentrance"
    }
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/pana-cc/mocha-typescript"
  },
  "scripts": {
    "build": "lerna run --stream build",
    "clean": "lerna run --stream clean",
    "coverage": "lerna run --stream coverage",
    "lint": "lerna run --stream lint",
    "setup": "npm run --stream setup:packages",
    "setup:packages": "npm install --no-package-lock && lerna bootstrap --hoist --no-ci",
    "test": "lerna run --stream test"
  },
  "dependencies": {
    "lerna": "^3.10.7"
  }
}
