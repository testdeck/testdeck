{
  "name": "mocha-typescript-di-typedi",
  "version": "1.0.0",
  "description": "mocha-typescript dependency injection support for typedi",
  "author": "Panayot Cankov",
  "contributors": [
    {
      "name": "silkentrance",
      "url": "https://github.com/silkentrance"
    }
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/pana-cc/mocha-typescript"
  },
  "main": "index.js",
  "typings": "index.d.ts",
  "scripts": {
    "build": "tsc",
    "changelog": "conventional-changelog --infile CHANGELOG.md --same-file --preset angular --append --release-count 1 --verbose",
    "clean": "rm -Rf coverage *.tgz *.js.map *.js test/*.js*",
    "coverage": "npm run clean && npm run build && nyc mocha test/*Test.ts --opts test/mocha.opts",
    "lint": "tslint --project .",
    "lint:fix": "tslint --fix --project .",
    "test": "npm run clean && npm run lint && npm run build && npm run test:run",
    "test:run": "mocha test/*Test.ts --opts test/mocha.opts"
  },
  "dependencies": {
    "typedi": "^0.8.0",
    "reflect-metadata": "^0.1.12"
  },
  "peerDependencies": {
    "mocha-typescript": "latest"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.1.4",
    "chai": "^4.2.0",
    "chalk": "^2.4.1",
    "contributor-faces": "^1.0.1",
    "conventional-changelog-cli": "^2.0.1",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "source-map-support": "^0.5.6",
    "ts-node": "^6.1.0",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  },
  "files": [
    "index.ts",
    "index.d.ts",
    "index.js",
    "index.js.map"
  ],
  "nyc": {
    "check-coverage": true,
    "lines": 95,
    "statements": 85,
    "functions": 95,
    "branches": 95,
    "include": [
      "**/*.js"
    ],
    "exclude": [
      "bin",
      "coverage",
      "test/**/*.js"
    ],
    "reporter": [
      "lcov",
      "text-summary"
    ],
    "all": true
  }
}
